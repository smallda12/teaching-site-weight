# 重量｜班級互動教學網　使用說明

數學　115 學年度 第 1 學期　第 2 單元

雙擊 `index.html` 就能用，**不需要安裝、不需要網路**（只有「🔗 延伸學習」那一區要連網）。

---

## 兩種版本

| 開啟方式 | 版本 | 差別 |
|---|---|---|
| 雙擊本機 `index.html` | **教室版**（右上角黃色標記） | 點名區可以上傳學生照片 |
| 用線上網址開啟 | **公開版**（右上角藍色標記） | 照片上傳自動關閉 |

🛑 **學生姓名與照片只存在那台電腦的瀏覽器裡**（localStorage／IndexedDB），
不會寫進任何檔案、也不會上傳到網路或 GitHub。

---

## 七個分區

| 分區 | 內容 |
|---|---|
| 🙋 點點名 | 照片＋姓名抽籤。**一輪每人只點一次**，全班點完就停；名單可匯出／匯入 JSON |
| 🃏 圖詞輪播 | 10 張學習圖卡，標題與說明各自是一顆發音鈕；可隨機抽或自動輪播 |
| 🎬 教學影片 | 完整版（約 4 分 48 秒）＋ **六個重點各一支主題短片**，右側清單想看哪段點哪段 |
| 📚 教材教學 | 六個重點全部開放、不必照順序；每個重點是「教學頁 → 3 題評量」，答對才過 |
| 📝 評量區 | 18 題總評量＋各重點正確率。**答錯不重答**，標出正解後直接下一題 |
| 🧠 自學區 | 圖卡點擊放大，可自動輪播整套 |
| 🔗 延伸學習 | 9 支外部教學影片（均一教育平台等），課後自己看。**需要網路** |
| 側邊工具 | 🖊️ 畫筆 12 色；⏱️ 三個各自獨立的計時器 |

---

## 六個教學重點

| # | 重點 | 教學頁 | 題號 |
|---|---|---|---|
| 1 | ⚖️ 認識公斤 | P04 | Q1–Q3 |
| 2 | 📦 公斤和公克 | P11 | Q4–Q6 |
| 3 | 🔍 看懂磅秤 | P18 | Q7–Q9 |
| 4 | 🕵️ 先猜再量比一比 | P40–P43 | Q10–Q12 |
| 5 | 🔄 公斤公克換算 | P44–P47 | Q13–Q15 |
| 6 | 🛒 生活應用與安全 | P48–P54 | Q16–Q18 |

正解序列 `A A B B A A B A B B A A B A B B A A`（A 10 題／B 8 題），
沒有連續三題相同、也沒有長段 ABAB 交錯——**學生沒辦法用位置記憶作答**。

---

## 給老師的幾個開關

- **🔊 自動朗讀**（教材教學區的進度卡）：關掉之後，切換頁面不會自動出聲，
  但按「🔊 再唸一次」還是會播。老師想自己講解時把它關掉。
- **自學區的自動輪播**：可選「只播標題」或「標題＋說明」，唸完才換下一張。
  點任何地方都會停止輪播。
- **三個計時器各自獨立**，各有自己的「時間到要說的話」，可以同時跑。

---

## 更新這個網站

```bash
python 工具腳本\建置素材_圖片壓縮.py     # 換了頁圖、影片、PDF
python 工具腳本\建置語音_全部.py         # 換了題目或圖卡文字
```

改完之後**一定要跳版本號**，否則瀏覽器會繼續用快取裡的舊檔：

| 改了什麼 | 要跳哪個 |
|---|---|
| 程式或樣式 | `index.html` 底部 5 個 `?v=` 各加 1 |
| 重產語音 | `js/config.js` 的 `語音.版本` 加 1 |
| 換頁圖／影片／PDF | `js/config.js` 的 `資源版本` 加 1 |

🛑 **題目要改就改單元資料夾裡的 `題庫.py`**，再依序重跑
`重生頁圖.py → 套用新頁圖.py → 重出簡報.py → 重建紙本.py → 產出文件.py → 產出data.js.py`。
`data.js` 是衍生檔，直接手改會讓網站與紙本、簡報對不上。

## 每學期開學前

```bash
python 工具腳本\檢查延伸影片.py
```

外部影片被下架或改成私人時，網站這邊**不會有任何徵兆**，只能定期檢查。
